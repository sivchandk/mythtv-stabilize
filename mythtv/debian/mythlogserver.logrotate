/var/log/mythtv/*.log {
	daily
	rotate 31
	notifempty
	delaycompress
	sharedscripts
	compress
	postrotate
        if [ -n "pidof mythlogserver" ]; then
    		pkill -HUP mythlogserver
        fi
	endscript
	su mythtv mythtv
}
